export type FeatureType = {
    title?: string,
    name?: string,
    release_date: string,
    overview: string,
    backdrop_path: string
    id: number
    vote_average: number
}

export type RowType = {
    title: string,
    fetchURL: string,
    id: string
}